# CLAUDE.md

GitHub profile README repo for NTCHz — the only content is `README.md`, rendered on github.com/NTCHz.

- No build/test/lint; edit `README.md` directly and push.
- `node_modules/` (bun-types only) is stray and gitignored — no package.json exists; don't add one.
- README uses external badge/stats services (shields.io, skillicons.dev, github-readme-stats) — URLs must stay absolute.
