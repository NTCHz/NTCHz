# {ชื่อโปรเจ็ค}

{1-2 บรรทัด: คืออะไร แก้ปัญหาอะไร ให้ใคร}

## Stack

{runtime + framework หลัก + DB — บรรทัดเดียวพอ เช่น "Bun + Next.js 16 + PostgreSQL (Drizzle)"}

## Setup

```bash
{คำสั่งติดตั้งจริง copy-paste ได้เลย}
cp .env.example .env   # แล้วใส่ค่า: {list env ที่ต้องมี ห้ามใส่ค่าจริง}
```

## Run

```bash
{dev command}
{test command}
{build command}
```

## Deploy

{deploy ที่ไหน ยังไง trigger อะไร — เช่น "push main → Coolify auto-deploy ที่ xxx.domain.com"}
